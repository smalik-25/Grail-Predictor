# Dependencies are added in the phase that first uses them, so this file
# never claims a library the code does not use yet.

# Phase 0
pytest>=8.0

# Phase 1: ingestion
requests>=2.31
beautifulsoup4>=4.12
trendspy>=0.1  # pytrends was archived April 2025; this is the maintained successor
praw>=7.7

# Phase 2a: entity resolution
rapidfuzz>=3.0
pandas>=2.0
pyarrow>=15.0

# Phase 3: warehouse
psycopg2-binary>=2.9
sqlglot>=23.0  # test-time DDL parsing so CI catches broken schema without a database

# Phase 4: transformation
dbt-postgres>=1.8

# Phases 5-6: labeling and features
# PySpark needs a JVM: Java 11 or 17 (brew install openjdk@17 on macOS)
pyspark>=3.5,<4.0
pandera>=0.20
setuptools>=69  # Python 3.12 removed distutils; pyspark 3.5 still imports it and setuptools shims it
